{
  "name": "@types/kue",
  "version": "0.11.0",
  "description": "Types for https://github.com/Automattic/kue",
  "scripts": {
    "lint": "tslint index.d.ts",
    "bundle": "typings bundle . -o bundle.d.ts",
    "test-spec": "ts-node test.ts",
    "test": "npm run lint && npm run bundle && npm run test-spec"
  },
  "devDependencies": {
    "kue": "^0.11.0",
    "ts-node": "^3.0.3",
    "tslint": "^5.2.0",
    "tslint-config-typings": "^0.3.1",
    "typescript": "^2.3.2",
    "typings": "^2.1.1"
  }
}
